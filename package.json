{
  "name": "aplaut_status",
  "description": "Статус сервисов Aplaut",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "statusfy dev",
    "build": "statusfy build",
    "start": "statusfy start",
    "generate": "statusfy generate",
    "new-incident": "statusfy new-incident",
    "delete-incident": "statusfy delete-incident",
    "update-incident": "statusfy update-incident",
    "gh-publish": "gh-pages -d dist",
    "deploy": "yarn generate && cp CNAME dist/ && yarn gh-publish"
  },
  "dependencies": {
    "statusfy": "^0.4.3"
  },
  "engines": {
    "node": ">=8.10",
    "npm": ">=5"
  },
  "devDependencies": {
    "@babel/preset-env": "7.12.17",
    "gh-pages": "^2.1.1"
  }
}
